# 角色系统缺口目标文档（基于 roledesign 对照实现）

更新时间：2026-02-27
对照来源：
- `docs/design/gamedesign/roledesign.md`
- `docs/technical/role-technical.md`
- `backend/app/models/schemas.py`
- `backend/app/services/world_service.py`
- `backend/app/api/routes.py`
- `frontend/src/types/app.ts`
- `frontend/src/components/PlayerPanel.tsx`
- `frontend/src/App.tsx`

## 结论摘要
- 当前已完成：玩家基础静态字段、DND5E 基础角色卡结构、NPC 角色池、NPC 单聊与结构化聊天记录、关系写入接口、基础行动检定接口。
- 当前未完成：`roledesign` 中大部分“通用角色属性”“背包/装备/BUFF/法术位/体力值”“角色关系联动”“角色完整编辑面板”“角色专项 API/AI 工具”。

## 待完成项目（按优先级）

### P0 数据结构补全（先补模型和存档兼容）
- [x] 补齐通用角色属性：经验（当前值/升级所需）、当前属性值与当前属性加值、是否死亡、角色状态。
- [x] 新增 BUFF 数据结构：名称、描述、持续时间、来源、叠加规则、剩余时间结算字段。
- [x] 新增背包与物品结构：金币、物品列表、物品基础枚举（名称/类型/描述/重量/稀有度/价值/效果/次数/冷却/绑定）。
- [x] 新增装备槽与装备状态：当前武器、当前护甲、可扩展装备位。
- [x] 新增法术与资源结构：法术池、法术位（总量/当前）、体力值（总量/当前）。
- [x] NPC/怪物与玩家统一角色骨架（当前仅 `PlayerStaticData` + `NpcRoleCard.profile`，怪物类型未形成独立结构）。
- [x] 地图区块强制重生成时执行存档清理：删除旧 `area_snapshot` 区块数据与旧 `role_pool` NPC 数据，避免遗留不可用脏数据持续累积。

### P0 规则引擎与计算落地
- [x] 实现 `roledesign` 约定的 AC/DC/当前值计算链路（当前主要是静态字段或简化检定，未形成统一计算层）。
- [x] 实现“所有当前值 = 基础值 + BUFF影响”的实时计算/刷新机制。
- [x] 将行动检定结果与角色资源系统打通（HP、法术位、体力值、BUFF时长推进），而非仅局部扣 HP。

### P1 API 与工具能力补齐
- [x] 角色属性获取与更新接口细化（按模块更新，避免仅整包 `player/static` 覆盖）。
- [x] 装备管理接口：更改角色武器/护甲。
- [x] BUFF 管理接口：增加/移除 BUFF。
- [x] 物品管理接口：给予/移除物品、物品使用。
- [x] 技能/法术管理接口：给予/移除技能与法术、法术位消耗/恢复。
- [x] 资源管理接口：体力值消耗/恢复。
- [x] 关系接口扩展：查询/更新关系，并接入聊天与行动结果。
- [x] 以上接口对应 AI tool schema 与后端工具执行映射（当前仅有限工具与 `/actions/check`）。

### P1 前端能力补齐
- [x] 玩家面板补全为“完整角色卡编辑”而非仅 ID/名称/移动速度。
- [x] 新增背包面板：装备、使用、查看物品详情与交互入口。
- [x] 新增 BUFF/状态展示区：持续时间、来源、即时影响。
- [x] 新增资源展示区：HP、体力、法术位、当前状态。
- [x] 保存后联动刷新依赖功能（地图移动耗时、行动检定参数、聊天上下文提示）。

### P2 NPC 关系与行为深化
- [x] NPC 关系自动演进：根据聊天/行动结果更新 relation tag（当前有建议值与手动 upsert，但未形成自动闭环）。
- [x] NPC 角色卡补充“认知变化、态度变化”的结构化记录。
- [x] NPC 对话上下文引入关系与近期事件权重（当前主要是日志拼接）。

### P2 验收与测试
- [x] 新增角色系统专项回归测试：模型默认值、旧存档补齐、字段边界校验。
- [x] 新增规则测试：AC/DC/BUFF叠加/资源消耗恢复。
- [x] 新增接口测试：装备、BUFF、背包、法术位、体力值、关系更新。
- [x] 新增前端交互测试：角色面板保存、背包使用、关系变化可视化。

## 里程碑建议
- M1.5（结构补齐）：先完成 P0 数据结构 + 兼容迁移，不改大交互。
- M2（规则可用）：完成 P0 规则引擎 + P1 核心接口。
- M3（体验闭环）：完成 P1 前端与 P2 关系/行为深化。
