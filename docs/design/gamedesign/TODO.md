# TODO

用于记录“先不做、后续在包装阶段实现”的设计事项。

## 聊天与工具调用

- [ ] 增加“自动重试”机制：当玩家输入包含明显移动意图（如“前往/移动到/进入”）但 AI 未调用 `move_to_zone` 时，后端自动判定本轮无效并重试一次。
- [ ] 增加“工具调用完整性校验”策略：按意图类型检查必须调用的工具集合（如移动前可先要求 `get_map_index`）。
- [ ] 增加“工具失败回退模板”：当工具调用失败时，统一返回可恢复引导语，避免叙事与状态脱节。
- 检测区域交互 ai tool,用于限制玩家与非法交互物交互
- 检测区域可交互 Npc
